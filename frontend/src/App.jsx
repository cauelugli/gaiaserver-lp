import { Button, Typography, Box } from '@mui/material';

function App() {
  return (
    <Box sx={{ textAlign: 'center', marginTop: 4 }}>
      <Typography variant="h2">Hello World com Vite!</Typography>
      <Button variant="contained" sx={{ marginTop: 2 }}>
        Material UI Button
      </Button>
    </Box>
  );
}

export default App;